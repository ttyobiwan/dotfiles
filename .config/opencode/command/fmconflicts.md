---
description: Resolve merge conflicts
agent: build
---

We are currently on the target branch. We just merged master to it, but there are couple of conflicts. Resolve them. No need to run `git add` or `git commit`.
